{% extends 'base.html.twig' %}


{% block title %}{{ category.name }}{% endblock %}
{% block headline %}{{ category.name }}{% endblock %}
{% block breadcrumb %}{{ category.name }}{% endblock %}

{% block body %}
    <div class="container">
        {% include 'post/_list.html.twig' with {'posts': pagination} %}
        <div class="navigation">
            {{ knp_pagination_render(pagination) }}
        </div>
    </div>
{% endblock %}
