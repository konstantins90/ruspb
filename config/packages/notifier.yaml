framework:
    notifier:
        chatter_transports:
            # Wenn TELEGRAM_DSN nicht gesetzt ist → null-Transport (keine Benachrichtigung)
            telegram: '%env(default:null://null:TELEGRAM_DSN)%'
        texter_transports:
        channel_policy:
            # use chat/slack, chat/telegram, sms/twilio or sms/nexmo
            urgent: ['email']
            high: ['email']
            medium: ['email']
            low: ['email']
        admin_recipients:
            - { email: admin@example.com }
